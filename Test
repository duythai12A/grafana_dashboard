{# render mọi key trừ kubelet-arg #}
{% set cfg_without_args = (cfg_merged | dict2items
                           | rejectattr('key','equalto','kubelet-arg')
                           | items2dict) %}
{{ cfg_without_args | to_nice_yaml(indent=2) | trim }}
kubelet-arg:
{% for a in cfg_merged['kubelet-arg'] | default([]) %}
  - "{{ a | replace('"','\"') }}"
{% endfor %}
