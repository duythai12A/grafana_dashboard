- name: Quote every item under kubelet-arg
  ansible.builtin.replace:
    path: "{{ rke2_config_path }}"
    regexp: '^(\s*-\s)([^"].*?)$'
    replace: '\1"\2"'
    after: '^kubelet-arg:\s*$'
    before: '^[^\s]'
