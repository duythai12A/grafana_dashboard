(
        label_replace(
          100 * (avg by (volume) (longhorn_volume_actual_size_bytes))
              / (avg by (volume) (longhorn_volume_capacity_bytes)),
          "persistentvolume", "$1", "volume", "(.*)"
        )
      )
      * on (persistentvolume)
        group_left(namespace, persistentvolumeclaim, storageclass)
        kube_persistentvolumeclaim_info
      > ể0
