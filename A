100 *
(
  sum by (volume, pvc, pvc_namespace) (longhorn_volume_actual_size_bytes)
  /
  clamp_min(sum by (volume, pvc, pvc_namespace) (longhorn_volume_capacity_bytes), 1)
)
* on (volume) group_left()
  max by (volume) (longhorn_volume_state == 2)
> 80
